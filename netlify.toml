[build]
  base      = "artifacts/anaya-studio"
  command   = "pnpm install --frozen-lockfile && pnpm run build"
  publish   = "dist/public"

[build.environment]
  NODE_VERSION = "20"
  BASE_PATH    = "/"
  PORT         = "3000"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
